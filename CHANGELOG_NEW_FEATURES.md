# Changelog - Обновление бота с новыми типами событий

## Версия 2.0 - Февраль 2026

### ✨ Новые функции

#### 1. Событие без текста (`/add_event_notext`)
- **Назначение**: Отслеживание сообщений (желательно фото) по расписанию
- **Синтаксис**: `/add_event_notext [начало] [конец]`
- **Пример**: `/add_event_notext 09:00 18:00`
- **Особенности**:
  - Отслеживает 1 сообщение от 1 пользователя в промежуток времени
  - Поддерживает пересылаемые сообщения
  - Строгий deadline (без дополнительного буфера в 5 минут)
  - Поддержка выходных дней через сообщение "выходной"
  - Статистика показывает только непустые разделы

#### 2. События с ключевым словом (`/add_event_open`, `/add_event_close`)
- **Назначение**: Отслеживание сообщений с конкретным ключевым словом
- **Синтаксис**: `/add_event_open [начало] [конец] "ключевое слово"`
- **Примеры**: 
  - `/add_event_open 09:00 18:00 "открыт"`
  - `/add_event_close 18:00 23:00 "закрыт"`
- **Особенности**:
  - Ключевое слово может быть в любом месте сообщения
  - Поддержка regex вариаций: "открыт" → открыт, открыта, открыто, открытие (до 5 символов)
  - Регистронезависимый поиск
  - Строгий deadline
  - Статистика показывает только непустые разделы

#### 3. Расширенная команда `/rm_event`
- **Что изменилось**: Теперь поддерживает удаление всех типов событий
- **Поддерживаемые типы**:
  - Обычные события (`/add_event`)
  - Временные события (`/add_tmp_event`)
  - Checkout события (`/add_event_checkout`)
  - **NoText события** (`/add_event_notext`) ← НОВОЕ
  - **Keyword события** (`/add_event_open`, `/add_event_close`) ← НОВОЕ

### 🔧 Исправления

#### 1. Время публикации статистики checkout
- **Проблема**: Статистика публиковалась с временем в UTC
- **Решение**: Изменено на UTC+3 (МСК)
- **Затронутая функция**: `send_checkout_stats_for_event` в `scheduler/tasks.py`

### 🗄️ Изменения базы данных

#### Новые таблицы:

**notext_events**
- `id` - уникальный идентификатор
- `channel_id` - связь с каналом
- `deadline_start` - начало отслеживания (TIME)
- `deadline_end` - конец отслеживания (TIME)
- `created_at` - дата создания

**notext_reports**
- `id` - уникальный идентификатор
- `user_id` - пользователь
- `notext_event_id` - событие
- `report_date` - дата отчета
- `submitted_at` - время отправки
- `message_id` - ID сообщения
- `is_on_time` - сдано вовремя (BOOLEAN)
- UNIQUE CONSTRAINT: (user_id, notext_event_id, report_date)

**notext_day_offs**
- `id` - уникальный идентификатор
- `user_id` - пользователь
- `notext_event_id` - событие
- `day_off_date` - дата выходного
- `created_at` - время создания
- UNIQUE CONSTRAINT: (user_id, notext_event_id, day_off_date)

**keyword_events**
- `id` - уникальный идентификатор
- `channel_id` - связь с каналом
- `deadline_start` - начало отслеживания (TIME)
- `deadline_end` - конец отслеживания (TIME)
- `keyword` - ключевое слово для поиска
- `created_at` - дата создания

**keyword_reports**
- `id` - уникальный идентификатор
- `user_id` - пользователь
- `keyword_event_id` - событие
- `report_date` - дата отчета
- `submitted_at` - время отправки
- `message_id` - ID сообщения
- `message_text` - текст сообщения
- `is_on_time` - сдано вовремя (BOOLEAN)
- UNIQUE CONSTRAINT: (user_id, keyword_event_id, report_date)

#### Обновленные relationships в моделях:

**User**
- Добавлены: `notext_reports`, `notext_day_offs`, `keyword_reports`

**Channel**
- Добавлены: `notext_events`, `keyword_events`

### 📝 Технические детали

#### Обработчики сообщений

**reports.py - handle_photo_message**
- Добавлена обработка notext событий
- Проверка временного окна отслеживания
- Проверка на выходной день
- Предотвращение повторной отправки

**reports.py - handle_checkout_first_phase** (переименован)
- Добавлена обработка выходных дней для notext событий
- Добавлена обработка keyword событий
- Проверка regex совпадений для ключевых слов
- Проверка временного окна отслеживания

#### CRUD операции

**crud.py - Новые классы**
- `NoTextEventCRUD`: управление notext событиями
- `NoTextReportCRUD`: управление отчетами notext
- `NoTextDayOffCRUD`: управление выходными днями notext
- `KeywordEventCRUD`: управление keyword событиями
- `KeywordReportCRUD`: управление отчетами keyword
- `match_keyword_regex()`: функция проверки regex совпадений

#### Планировщик задач

**tasks.py - Новые функции**
- `check_notext_keyword_events()`: проверка времени публикации статистики
- `send_notext_stats()`: формирование и отправка статистики notext
- `send_keyword_stats()`: формирование и отправка статистики keyword

**tasks.py - Новые задачи**
- Задача `check_notext_keyword_events` запускается каждую минуту
- Публикация статистики строго в `deadline_end` (± 1 минута)

### 🎯 Формат статистики

#### NoText событие
```
📊 Статистика отправки фото

✅ Сдали вовремя:
- @user1
- @user2

❌ Не сдали:
- @user3

🏖 Выходной:
- @user4
```

#### Keyword событие
```
📊 Статистика по ключевому слову 'открыт'

✅ Сдали вовремя:
- @user1
- @user2

❌ Не сдали:
- @user3
```

**Важно**: Пустые разделы не отображаются!

### 🧪 Тестирование

Для тестирования новых функций:

1. **NoText событие**:
   ```
   /add_event_notext 09:00 18:00
   # Отправить фото в промежуток 09:00-18:00
   # Или написать "выходной" для пропуска
   # Статистика опубликуется в 18:00
   ```

2. **Keyword событие**:
   ```
   /add_event_open 09:00 12:00 "открыт"
   # Написать "открыт", "открыта", "открыто" и т.д.
   # Статистика опубликуется в 12:00
   ```

3. **Удаление события**:
   ```
   /rm_event
   # Выбрать событие из списка (включая новые типы)
   ```

### 📦 Файлы для обновления

Необходимо заменить следующие файлы:
- `bot/database/models.py` - новые модели таблиц
- `bot/database/crud.py` - новые CRUD операции
- `bot/handlers/admin.py` - новые команды
- `bot/handlers/reports.py` - обработка новых событий
- `bot/scheduler/tasks.py` - публикация статистики
- `migrate_db.py` - скрипт миграции

### 🔐 Безопасность

- Все новые события поддерживают проверку прав пользователя
- Используются те же механизмы авторизации, что и в существующих событиях
- Предотвращение повторной отправки через UNIQUE CONSTRAINT

### ⚡ Производительность

- Проверка событий выполняется каждую минуту (как и для других типов)
- Использование индексов на полях date для быстрого поиска
- Минимальные запросы к БД благодаря правильным relationships

### 🐛 Известные ограничения

1. NoText событие принимает только 1 сообщение в день от пользователя
2. Keyword события проверяют только текст сообщения (не caption к фото)
3. Regex ограничен 5 символами после базового слова
4. Статистика публикуется только в окне ± 1 минута от deadline_end

### 📚 Документация

Полная документация доступна в файле `UPDATE_INSTRUCTIONS.md`
