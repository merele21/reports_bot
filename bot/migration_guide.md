# Руководство по миграции на версию 2.0

## 🎯 Основные изменения

### 1. Автоматические миграции БД
- ✅ Миграции теперь применяются автоматически при запуске бота
- ✅ Не нужно вручную запускать `python migrate_db.py`
- ✅ Данные сохраняются между перезапусками

### 2. Разделение прав администраторов
- **Суперадмины**: могут добавлять/удалять администраторов
- **Администраторы**: управляют каналами, пользователями, отчетами

### 3. Улучшенная работа с шаблонами фото
- Пошаговый процесс добавления:
  1. `/add_template` - начало
  2. Отправка фото
  3. `/done` - завершение фото
  4. Описание шаблона (опционально)

### 4. Новая архитектура событий
- Разделение каналов и событий
- Возможность редактирования параметров
- Удаление событий без удаления канала

### 5. Hot Reload для разработки
- Автоматическая перезагрузка при изменении кода
- Запуск через `dev.bat` (Windows) или `dev.sh` (Linux/Mac)

## 📦 Процесс обновления

### Шаг 1: Обновление кода

```bash
# Сохраните изменения в вашем .env файле
cp .env .env.backup

# Обновите код
git pull origin main
# или скопируйте новые файлы
```

### Шаг 2: Обновление зависимостей

```bash
# Обновите requirements.txt новой версией
pip install -r requirements.txt

# Или с использованием Docker
docker-compose build --no-cache
```


### Шаг 4: Запуск бота

```bash
# Обычный запуск (миграции применятся автоматически)
python -m bot.main

# Или через Docker
docker-compose up -d

# Для разработки с hot reload
./dev.sh  # Linux/Mac
dev.bat   # Windows
```

## 🔄 Что происходит при первом запуске

1. **Автоматическая миграция БД**:
   - Создаются новые колонки в существующих таблицах
   - Создаются новые таблицы (если их нет)
   - Существующие данные сохраняются

2. **Проверка целостности**:
   - Все зарегистрированные каналы остаются активными
   - Все пользователи сохраняются в системе
   - История отчетов не теряется

3. **Новые возможности**:
   - Команда `/help` показывает доступные команды
   - FSM states для пошагового добавления шаблонов
   - Разделение прав администраторов

## 📝 Новые команды

### Для всех пользователей
```
/get_user_id     - Узнать свой Telegram ID
/get_chat_id     - Узнать Chat ID (лимит: 1/день)
/get_thread_id   - Узнать Thread ID (лимит: 1/день)
/help            - Показать доступные команды
```

### Для администраторов
```
/add_ch          - Зарегистрировать канал/тред
/add_event       - Добавить событие отчета
/edit_event      - Редактировать событие
/rm_event        - Удалить событие
/add_user        - Добавить пользователя
/add_users       - Добавить несколько (@user1;@user2)
/rm_user         - Удалить пользователя
/rm_users        - Удалить несколько
/rm_ch           - Удалить канал
/add_template    - Добавить шаблон фото (FSM)
/set_stats_destination - Настроить статистику
/edit_wstat      - Редактировать еженедельную статистику
/list_channels   - Список каналов
/stats           - Статистика напоминаний
```

### Для суперадминистраторов
```
/add_admin       - Добавить администратора
/rm_admin        - Удалить администратора
/list_admins     - Список администраторов
```

## 🛠 Изменения в рабочем процессе

### Старый процесс:
```
1. /add_ch название 10:00 ключевое_слово 2
2. /add_user (reply)
3. /set_stats_destination
```

### Новый процесс:
```
1. /add_ch название_треда
2. /add_event название 10:00 ключевое_слово 2
3. /add_user (reply)
4. /add_template
   - Отправить фото
   - /done
   - Описание (или /skip)
5. /set_stats_destination
```

## 🔧 Отладка

### Проблема: Бот не видит данные после обновления

**Решение**:
```bash
# Проверьте логи
docker-compose logs -f bot

# Убедитесь, что миграции применились
# В логах должно быть:
# "✅ Database migrations completed successfully"
```

### Проблема: Hot reload не работает

**Решение**:
```bash
# Установите watchdog
pip install watchdog

# Linux/Mac: сделайте скрипт исполняемым
chmod +x dev.sh

# Запустите
./dev.sh  # или dev.bat на Windows
```

### Проблема: Пользователи не получают напоминания

**Решение**:
- Проверьте, что пользователи добавлены через `/add_user`
- Убедитесь, что событие создано через `/add_event`
- Проверьте, что бот админ в канале/треде

## 📊 Проверка успешной миграции

После запуска выполните:

```bash
# Проверьте список каналов
/list_channels

# Проверьте команды
/help

# Для SQLite проверьте БД
sqlite3 bot_data.db
.tables
# Должны быть: users, channels, user_channels, photo_templates, reports, stats
```

## 🆘 Откат изменений

Если что-то пошло не так:

```bash
# 1. Остановите бота
docker-compose down  # или Ctrl+C

# 2. Восстановите .env
mv .env.backup .env

# 3. Восстановите базу данных (если есть backup)
cp bot_data.db.backup bot_data.db

# 4. Вернитесь к предыдущей версии кода
git checkout previous_version
```

## ✅ Контрольный список

- [ ] Создан backup .env файла
- [ ] Создан backup базы данных
- [ ] Установлены новые зависимости
- [ ] Добавлен SUPER_ADMIN_IDS в .env
- [ ] Бот успешно запустился
- [ ] Миграции применились автоматически
- [ ] `/help` показывает новые команды
- [ ] `/list_channels` показывает существующие каналы
- [ ] Тестовый пользователь успешно добавлен
- [ ] Тестовое событие создано
- [ ] Hot reload работает (для dev)

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи: `docker-compose logs -f bot`
2. Убедитесь в правах бота в каналах
3. Проверьте формат переменных в `.env`
4. Создайте issue с логами и описанием проблемы