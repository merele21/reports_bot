# 📢 Функция предупреждений о приближении дедлайна

## 🎯 Что добавлено

Теперь бот отправляет **два типа уведомлений**:

1. **⚠️ Предупреждение ДО дедлайна** (новое!)
   - Отправляется за N минут до наступления дедлайна
   - Напоминает участникам, что времени осталось мало
   - Список тех, кто ещё не сдал отчет

2. **🔴 Напоминание ПОСЛЕ дедлайна** (было раньше)
   - Отправляется через 5 минут после дедлайна
   - Список должников
   - Записывается в статистику

---

## ⚙️ Настройка

### 1. Добавьте в `.env` файл:

```env
# За сколько минут ДО дедлайна отправлять предупреждение
DEADLINE_WARNING_MINUTES=5
```

**Примеры:**
- `DEADLINE_WARNING_MINUTES=5` → предупреждение за 5 минут
- `DEADLINE_WARNING_MINUTES=10` → предупреждение за 10 минут
- `DEADLINE_WARNING_MINUTES=15` → предупреждение за 15 минут

### 2. Замените файлы:

Замените следующие файлы обновленными версиями:

```bash
# 1. Планировщик
cp scheduler_tasks_updated.py bot/scheduler/tasks.py

# 2. Конфигурация
cp config_updated.py bot/config.py

# 3. Пример .env (опционально)
cp env_local_example_updated.txt .env.local.example
```

### 3. Перезапустите бота:

```bash
# Docker
docker-compose restart bot

# Или локально
python -m bot.main
```

---

## 📝 Как это работает

### Пример с дедлайном в 12:00

```
DEADLINE_WARNING_MINUTES=5

Timeline:
11:55:00 ⚠️ ПРЕДУПРЕЖДЕНИЕ (новое!)
         "До дедлайна осталось 5 минут!"
         + список тех, кто не сдал

12:00:00 ⏰ ДЕДЛАЙН

12:05:00 🔴 НАПОМИНАНИЕ (как было)
         "Напоминаю, что необходимо сдать отчет!"
         + запись в статистику
```

### Сообщение предупреждения выглядит так:

```
⚠️ ВНИМАНИЕ! До дедлайна осталось 5 минут! ⚠️

📋 Отчет: Касса
🔑 Ключевое слово: касса
⏰ Дедлайн: 12:00
📸 Минимум фото: 2

Еще не сдали отчет:
1. @username1
2. @username2
3. Иван Иванов

⏱ Поторопитесь, времени мало!
```

---

## 🔧 Технические детали

### Логика работы:

1. **Каждую минуту** планировщик проверяет все события
2. Для каждого события вычисляет `warning_time = deadline - DEADLINE_WARNING_MINUTES`
3. Если текущее время попадает в минуту `warning_time`, отправляет предупреждение
4. Кэширует отправленные предупреждения (чтобы не дублировать)
5. В начале нового дня кэш очищается

### Кэширование:

```python
# Каждое предупреждение кэшируется по ключу:
key = (channel_id, event_id, today_date)

# Пример:
(42, 15, date(2024, 01, 18))

# Это значит: канал 42, событие 15, дата 18.01.2024
# Один раз в день на это событие
```

### Проверка должников:

Перед отправкой проверяется:
- Есть ли у пользователя отчет на сегодня? (`ReportCRUD.get_today_report`)
- Если НЕТ → добавляем в список должников
- Если список пустой → ничего не отправляем

---

## 🧪 Тестирование

### Быстрый тест:

1. Создайте событие с дедлайном через 7 минут:

```
/add_event тест тест 14:37 1
```

2. Добавьте пользователей:

```
/add_users @user1 @user2
```

3. В `.env` установите:

```env
DEADLINE_WARNING_MINUTES=5
```

4. Ждите:
   - **14:32** → придет предупреждение
   - **14:42** → придет напоминание (если не сдали)

### Проверка логов:

```bash
# Смотрим логи планировщика
docker-compose logs -f bot | grep -i "warning\|deadline"

# Должны увидеть:
# "Warning sent for channel Test, event тест, 2 users"
# "Warning message sent to channel -100..."
```

---

## 🎨 Кастомизация

### Изменить текст предупреждения:

Отредактируйте метод `send_warning_message` в `bot/scheduler/tasks.py`:

```python
text = (
    f"⚠️ <b>ВАШ ТЕКСТ!</b> ⚠️\n\n"
    f"📋 Отчет: <b>{channel.title}</b>\n"
    # ... остальное
)
```

### Добавить эмодзи или форматирование:

```python
debt_list = [
    f"❌ {i}. @{u.username}" if u.username else f"❌ {i}. {u.full_name}"
    for i, u in enumerate(debtors, 1)
]
```

### Изменить время предупреждения для конкретного канала:

В будущем можно добавить поле `warning_minutes` в модель `Event`:

```python
class Event(Base):
    # ...
    warning_minutes: Mapped[int] = mapped_column(Integer, default=5)
```

Затем в планировщике:

```python
warning_minutes = ev.warning_minutes  # Вместо settings.DEADLINE_WARNING_MINUTES
```

---

## 🐛 Troubleshooting

### Предупреждения не приходят

**Проверьте:**

1. Логи бота:
```bash
docker-compose logs -f bot | grep -i warning
```

2. Есть ли активные события:
```
/list_channels
```

3. Правильное ли время:
```python
# В планировщике добавьте логи:
logger.info(f"Now: {now}, Warning time: {warning_time}, Deadline: {deadline}")
```

4. Timezone корректный:
```env
TZ=Europe/Moscow  # Ваш часовой пояс
```

### Дублируются сообщения

Это невозможно благодаря кэшу `warnings_sent_today`, но если происходит:

1. Проверьте, что планировщик не запущен дважды
2. Убедитесь, что кэш не очищается раньше времени

### Приходят в неправильное время

Проверьте:
```env
DEADLINE_WARNING_MINUTES=5  # Не опечатка ли?
```

И логи:
```bash
# Смотрим вычисленное время предупреждения
docker-compose logs bot | grep "warning_time\|Warning sent"
```

---

## 📊 Статистика

Предупреждения **НЕ** записываются в статистику (только напоминания после дедлайна).

Если хотите отслеживать:

```python
# В send_warning_message добавьте:
for u in debtors:
    # Можно создать отдельную таблицу WarningStats
    await WarningStatsCRUD.add_warning(session, u.id, channel.id)
```

---

## 🚀 Дополнительные идеи

### 1. Несколько предупреждений

Хотите отправлять за 10 и за 5 минут?

```python
warning_times = [10, 5]  # минуты

for minutes in warning_times:
    warning_time = deadline - timedelta(minutes=minutes)
    if warning_time <= now < warning_time + timedelta(minutes=1):
        # отправка
```

### 2. Персональные напоминания в ЛС

Вместо группового сообщения — каждому лично:

```python
for debtor in debtors:
    try:
        await self.bot.send_message(
            chat_id=debtor.telegram_id,
            text=f"⚠️ {debtor.full_name}, до дедлайна осталось {minutes_left} минут!"
        )
    except Exception:
        pass  # Пользователь заблокировал бота
```

### 3. Прогресс-бар

Показать, сколько % людей уже сдали:

```python
total = len(users)
submitted = total - len(debtors)
percent = (submitted / total) * 100

text = f"📊 Прогресс: {submitted}/{total} ({percent:.0f}%)"
```

---

## ✅ Итоговый чеклист

- [ ] Обновил `bot/scheduler/tasks.py`
- [ ] Обновил `bot/config.py`
- [ ] Добавил `DEADLINE_WARNING_MINUTES` в `.env`
- [ ] Перезапустил бота
- [ ] Создал тестовое событие
- [ ] Получил предупреждение за N минут до дедлайна
- [ ] Проверил логи планировщика

---

## 📞 Поддержка

Если что-то не работает:

1. Проверьте логи: `docker-compose logs -f bot`
2. Убедитесь, что время в системе правильное: `date`
3. Проверьте timezone: `echo $TZ`
4. Создайте issue с логами