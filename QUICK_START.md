# Быстрый старт - Обновление бота

## ⚡ Что нужно сделать

### 1. Остановите бота
```bash
sudo systemctl stop report-bot
# или завершите процесс вручную
```

### 2. Сделайте бэкап
```bash
cd /путь/к/боту
cp bot_data.db bot_data.db.backup
```

### 3. Распакуйте обновление
```bash
tar -xzf report-bot-updated.tar.gz
cd report-bot-local
```

### 4. Запустите миграцию
```bash
python migrate_db.py
```

### 5. Запустите бота
```bash
sudo systemctl start report-bot
# или запустите вручную: python -m bot.main
```

## 🎯 Новые команды

### Событие без текста (фото по расписанию)
```bash
/add_event_notext 09:00 18:00
```
- Бот отслеживает отправку фото с 09:00 до 18:00
- Статистика публикуется в 18:00
- Для выходного: написать "выходной"

### События с ключевым словом
```bash
/add_event_open 09:00 18:00 "открыт"
/add_event_close 18:00 23:00 "закрыт"
```
- Бот ищет ключевое слово в сообщениях
- Поддерживает вариации: открыт/открыта/открыто/открытие
- Статистика публикуется в конце периода

### Удаление событий
```bash
/rm_event
```
- Теперь удаляет ВСЕ типы событий, включая новые

## 📊 Формат статистики

**NoText событие:**
```
✅ Сдали вовремя:
- @user1
❌ Не сдали:
- @user2
🏖 Выходной:
- @user3
```

**Keyword событие:**
```
📊 Статистика по ключевому слову 'открыт'
✅ Сдали вовремя:
- @user1
❌ Не сдали:
- @user2
```

**Важно**: Пустые разделы не показываются!

## 🔍 Проверка работы

1. Создайте notext событие: `/add_event_notext 09:00 18:00`
2. Отправьте фото в промежутке 09:00-18:00
3. В 18:00 будет опубликована статистика

## 📚 Полная документация

Смотрите файлы:
- `UPDATE_INSTRUCTIONS.md` - подробная инструкция по обновлению
- `CHANGELOG_NEW_FEATURES.md` - полное описание изменений

## ⚠️ Если что-то пошло не так

1. Восстановите бэкап: `cp bot_data.db.backup bot_data.db`
2. Проверьте логи: `journalctl -u report-bot -f`
3. Убедитесь, что миграция прошла успешно
